

let visitorAge = 12;
let isStudent = true;
let cena = 10;


function printTicketPrice (visitorAge, isStudent) {
    return visitorAge <= 12;
}

console.log(printTicketPrice(visitorAge, isStudent))

function printTicketPrice (visitorAge, isStudent) {
    return visitorAge >= 35;
}

console.log(printTicketPrice(visitorAge, isStudent))